name: ghostweight
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch>=2.5.0
    - transformers>=4.47.0
    - accelerate>=0.26.0
    - bitsandbytes>=0.43.0
    - peft>=0.8.0
    - numpy>=1.26.0
    - ninja